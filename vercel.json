{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "react",
  "rootDirectory": "frontend",
  "env": {
    "REACT_APP_API_URL": {
      "description": "Base URL for backend API calls (e.g., https://your-backend.onrender.com)"
    }
  }
}
